package br.com.confirmar.model.impl;

import java.util.List;
import org.springframework.data.jpa.repository.JpaRepository;
import br.com.confirmar.model.Convidados;

public interface IConvidadosRepository {// extends JpaRepository<Convidados, Long> {
 //   List<Convidados> findByQuestionId(Long questionId);
}
